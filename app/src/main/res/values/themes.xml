<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlaylistMaker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/yp_blue</item>
        <item name="colorSecondary">@color/yp_blue</item>
    </style>
    <style name="Theme.PlaylistMaker" parent="Base.Theme.PlaylistMaker" />
    <!-- Стиль для кнопок на Главном экране -->
    <!-- Наследуемся от UnelevatedButton, чтобы убрать тень! -->
    <style name="MainScreenButtonStyle" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:textSize">@dimen/text_size_22</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">@dimen/button_corner_radius</item>
        <item name="iconGravity">textStart</item>
        <item name="iconPadding">8dp</item>
        <!-- Добавляем новый скачанный шрифт (Medium для кнопок) -->
        <item name="android:fontFamily">@font/ys_display_medium</item>

        <!-- Цвета оставляем жесткими, так как по макету эти кнопки белые даже в Темной теме -->
        <item name="backgroundTint">@color/yp_white</item>
        <item name="android:textColor">@color/yp_black</item>
        <item name="iconTint">@color/yp_black</item>
    </style>
    <!-- Стиль для контейнера (строки) настроек -->
    <style name="SettingsItemContainerStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/settings_item_height</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingHorizontal">@dimen/padding_standard</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <!-- Стиль для текста в строке настроек -->
    <style name="SettingsItemTextStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">@dimen/text_size_regular</item>
        <item name="android:fontFamily">@font/ys_display_regular</item>
        <item name="android:textColor">@color/primary_text_color</item>
    </style>

    <!-- Стиль для иконок-стрелочек в строке настроек -->
    <style name="SettingsItemIconStyle">
        <item name="android:layout_width">@dimen/icon_size</item>
        <item name="android:layout_height">@dimen/icon_size</item>
    </style>
    <style name="MainTitleTextStyle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">@dimen/text_size_title</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/ys_display_medium</item>
    </style>
</resources>